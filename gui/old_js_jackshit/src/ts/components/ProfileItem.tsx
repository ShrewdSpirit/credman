import React, { Component } from 'react'
import { view } from 'react-easy-state'
import styles from '../../styles/components/ProfileItem.less'

class ProfileItem extends Component {
    render(): JSX.Element {
        return <div className={styles.container}>dada</div>
    }
}

export default view(ProfileItem)
